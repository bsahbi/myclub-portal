export const metadata = {
  title: 'Saved & Offline',
  description: 'Bookmarked articles available offline.',
};

import { SavedOfflineView } from '@/components/SavedOfflineView';

export default function SavedPage() {
  return <SavedOfflineView />;
}
