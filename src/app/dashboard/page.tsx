export const metadata = {
  title: 'Dashboard',
  description: 'Analytics and platform performance insights.',
};

import { DashboardView } from '@/components/DashboardView';

export default function DashboardPage() {
  return <DashboardView />;
}
